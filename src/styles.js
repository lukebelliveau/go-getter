// @flow
const centerFlex = {
  display: 'flex',
  flexWrap: 'wrap',
  justifyContent: 'center',
  margin: 20,
};

const mobileDeviceWidth = '480px';

const primaryColor = '#D50000';
const hoverColor = '#A80A00';

export default {
  centerFlex,
  mobileDeviceWidth,
  primaryColor,
  hoverColor,
};